// Define the function displayMessage that accepts one parameter 'message'
function displayMessage(message) {
    // Print the 'message' parameter to the console
    console.log(message);
}

// Export the displayMessage function so it can be used in other files
module.exports = displayMessage;
